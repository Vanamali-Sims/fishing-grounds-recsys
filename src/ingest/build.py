"""Entry point: ``python -m src.ingest.build``.

Implementation comes in A1 — streamed zip (or extracted daily CSVs) to
year-month partitioned Parquet under ``data/processed/interactions/``.
"""


def main() -> None:
    raise NotImplementedError("A1 ingest is not implemented yet.")


if __name__ == "__main__":
    main()
