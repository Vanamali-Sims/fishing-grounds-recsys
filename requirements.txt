# A1 ingest: streamed zip/CSV → partitioned Parquet, peak-memory logging
duckdb==1.5.5
pyarrow==25.0.1
psutil==7.2.2

# A2 EDA (notebooks/ only)
pandas==3.0.5
matplotlib==3.11.1
jupyter==1.1.1
ipykernel==7.3.0

# A3 spatial joins (python -m src.features.build — not implemented yet)
geopandas>=1.0
rasterio>=1.4
shapely>=2.0

# C1 API stub
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
httpx==0.28.1
